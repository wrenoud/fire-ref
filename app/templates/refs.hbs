<table class="table">
	<thead>
		<tr>
			<th>Author(s)</th>
			<th>Year</th>
			<th>Title</th>
		</tr>
	</thead>
	<tbody>
	{{#each}}
		<tr>
			<td>{{author}}</td>
			<td>{{year}}</td>
			<td>{{#linkTo 'ref' this}}
				{{title}}
			{{/linkTo}}</td>
		</tr>
	{{/each}}
	</tbody>
</table>

{{outlet}}